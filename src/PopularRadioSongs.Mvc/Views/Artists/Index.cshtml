@model PagedViewModel<List<PopularRadioSongs.Application.UseCases.Artists.GetArtistsList.GroupArtistListDto>>

@{
    ViewData["Title"] = "Artists by Name";
}

<h1>Artists by Name</h1>

<table class="table">
    <tbody>
        @foreach (var artistGroup in Model.Value)
        {
            <tr>
                <td>
                    @artistGroup.Letter
                </td>
                <td>
                </td>
            </tr>
            @foreach (var artist in artistGroup.Artists)
            {
                <tr>
                    <td>
                    </td>
                    <td>
                        <a asp-controller="Artists" asp-action="Details" asp-route-artistId="@artist.Id">@artist.Name</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>

<partial name="_PaginationPartial" for="PagedData" />